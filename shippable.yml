env:
  global:
    - OPENSHIFT_REPO=ssh://545dc1c6e0b8cdc3460000dc@blueprint-neependra.rhcloud.com/~/git/blueprint.git

language: python

python:
  - 2.6
  - 2.7

# RethinkDB binds to 127.0.0.1 by default
#services:
#  - rethinkdb

install:
  - pip install -r requirements.txt

# Make folders for the reports
before_script:
  - mkdir -p shippable/testresults
  - mkdir -p shippable/codecoverage
  - git remote -v | grep ^openshift || git remote add openshift $OPENSHIFT_REPO
  - cd wsgi

script:
  - py.test

after_success:
  - git push -f openshift $BRANCH:master

archive: true
